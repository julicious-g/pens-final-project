import 'package:flutter/material.dart';

class FertilizerRecommendationPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Fertilizer Recommendation Page"),
    );
  }
}
